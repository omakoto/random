{
  "name": "stl-viewer",
  "version": "1.0.0",
  "description": "WebGL-based 3D model viewer for GitHub Pages",
  "scripts": {
    "dev": "parcel src/index.html --host 0.0.0.0",
    "build": "parcel build src/index.html --dist-dir docs --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.13.3",
    "typescript": "^5.8.2"
  }
}
